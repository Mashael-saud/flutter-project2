import 'package:flutter/material.dart';

class dayPage extends StatelessWidget {
  const dayPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Color(0xfff181a28),
    );
  }
}
